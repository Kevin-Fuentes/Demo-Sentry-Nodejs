# Copy to .env and load it (e.g. `node -r dotenv/config server.js`) or export
# it in your shell before starting the server.
# Get a DSN by creating a project at https://sentry.io (Platform: Node.js / Express).
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
PORT=3001
